import React from "react";
//1. In ActionTypes, I am adding three action types which i want to perform.
//this section, I will add actiontypes, for better naming convention,
//always use uppercase.Now, lets go and create the actions to Action folder.

const ActionTypes = {
  SET_PRODUCTS:"SET_PRODUCTS",
  SELECTED_PRODUCTS:"SELECTED_PRODUCTS",
  REMOVE_PRODUCTS:"REMOVE_PRODUCTS",
};

export default ActionTypes;
